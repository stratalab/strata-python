caption: Append many events in one commit.
steps:
  - call: event.batch_append
    args:
      entries:
        - { event_type: user.created, payload: { id: 1 } }
        - { event_type: user.updated, payload: { id: 2 } }
  - call: event.count
    args: {}
    returns: 2
