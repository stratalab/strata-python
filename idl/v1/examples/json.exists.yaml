caption: Check whether a document exists.
steps:
  - call: json.set
    args: { key: user, path: "$", value: { name: alice } }
  - call: json.exists
    args: { key: user }
    returns: true
  - call: json.exists
    args: { key: absent }
    returns: false
