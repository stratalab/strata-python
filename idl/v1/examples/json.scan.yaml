caption: Scan documents from the start, in key order.
steps:
  - call: json.set
    args: { key: a, path: "$", value: { v: 1 } }
  - call: json.set
    args: { key: b, path: "$", value: { v: 2 } }
  - call: json.scan
    args: {}
    returns: ["a", "b"]
