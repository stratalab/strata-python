caption: Create a secondary index on a JSON field.
steps:
  - call: json.index.create
    args: { name: by_name, field_path: "$.name", index_type: tag }
  - call: json.index.list
    args: {}
    returns: ["by_name"]
