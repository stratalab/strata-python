caption: Read many keys at once; a missing key comes back as null.
steps:
  - call: kv.batch_put
    args:
      entries:
        - { key: a, value: "1" }
        - { key: b, value: "2" }
  - call: kv.batch_get
    args: { keys: [a, b, missing] }
    returns: ["1", "2", null]
